# Artifact Inventory Test Fixture

A small project exercising the repository-artifact layer: a Maven manifest, config
files (properties + YAML), a Dockerfile, a Kubernetes manifest, a CI workflow, and a
Java source file that must **not** appear in the inventory.
